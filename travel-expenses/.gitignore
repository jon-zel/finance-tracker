.venv/
__pycache__/
*.pyc
server.log

# Deployment artifacts (deployment-auth-spec §7, §8) — regenerated by
# deploy.bat, never committed.
config/.session_secret
tools/
logs/

# User's personal trip data and rate history — not for version control.
trips/**/*.xlsx
config/rates_history.json
